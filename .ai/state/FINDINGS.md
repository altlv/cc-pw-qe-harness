# FINDINGS — claim ledger

| Claim                                                                     | Type     | Evidence                                                                    | Confidence | Consequence                                                            |
| ------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------- |
| FakeRestAPI POST /Books returns 200 and echoes input but does NOT persist | OBSERVED | `POST /api/v1/Books {id:9999}` -> 200 echo; `GET /api/v1/Books/9999` -> 404 | high       | Any create-then-read test must be marked `test.fail()` as known-broken |
| FakeRestAPI returns 200 on create, not 201                                | OBSERVED | curl status=200 on POST /api/v1/Books                                       | high       | Contract smell worth documenting                                       |
| FakeRestAPI 404s carry RFC7231 problem+json                               | OBSERVED | GET /api/v1/Books/200000 body has type/title/status/traceId                 | high       | Assertable error contract                                              |
| GitHub API unauthenticated rate limit is 60/hr                            | OBSERVED | `X-RateLimit-Limit: 60` on GET /repos/microsoft/playwright                  | high       | Tests must authenticate or skip, else flake                            |
| GitHub repos API returns ETag; supports conditional requests              | OBSERVED | `ETag: W/"769f23..."` response header                                       | high       | Enables 304 conditional-request tests                                  |
| Countdown timer `reset` does not change running state                     | OBSERVED | probe: reset while running then runFor(10s) -> 00:00:35                     | high       | Corrected a wrong test assumption                                      |
| Playwright `clock.fastForward` fires each pending timer once              | OBSERVED | fastForward(4000) advanced display 1s; runFor(5000) advanced 5s             | high       | Must use runFor for recursive setTimeout apps                          |
| Network events reach Node after the DOM updates                           | OBSERVED | 1-in-60 failure asserting on entries() straight after an action             | high       | waitForCall() polling is required                                      |
| `roles.ts` behaves as specified                                           | UNKNOWN  | never executed                                                              | —          | Do not claim `cc` is covered                                           |
| Triage agent works against the live API                                   | UNKNOWN  | no API key present in env or .env                                           | —          | README must keep saying unverified                                     |
| `.gitignore` rule `reports/` (unanchored) also ignored `examples/reports/` | OBSERVED | `git check-ignore -v` named .gitignore:25; `git ls-files examples/` showed only failure-403.json | high | The example report CI validates was never committed — first CI run failed |
| `check-report` crashed with an unhandled ENOENT on a missing path | OBSERVED | CI log: `ENOENT ... open '.../examples/reports'` at check-report.ts:30 | high | Fixed: stat first, clear message, exit 2 for an explicit path |
| A missing explicit path exited 0 before the fix | OBSERVED | local repro: `check-report does-not-exist` printed a message and exited 0 | high | That is how a CI step could "pass" while checking nothing |
| `--reporter=line` on the CLI replaces the reporters configured in playwright.config.ts | OBSERVED | after a `--reporter=line` run, artifacts/results.json mtime was unchanged | high | The gate silently read hours-old results and reported on a world that no longer existed |
| The gate had no staleness check | OBSERVED | rendered "No tests ran / 56 skipped" while 61 tests had just passed | high | Fixed: refuses results older than the newest source file, exit 2 |
| `mutate` treated a spawn failure as a failing suite | OBSERVED | `npx.cmd` via execFile gave EINVAL; baseline reported "suite fails" | high | Would have scored 100% having run nothing. Now refuses to score. |
| Two gate integration tests shared `artifacts/verdict.json` | OBSERVED | reproduced 1-in-6 with `--repeat-each=3`; 0/24 after giving each its own path | high | My own banned anti-pattern, in tests written the same hour |
| A role-contract assertion using `/rather than/` asserted nothing | OBSERVED | mutation stripping "Not for ..." survived; the phrase occurs in ordinary prose | high | Tightened to `not for`; mutation now caught, 14/14 |
| Shell heredocs have written invisible characters three times | OBSERVED | 2 BOMs, then 2 backspace chars in roles.test.ts | medium | Use a script file for edits containing escapes, not a heredoc |
| 93% of tests explained nothing on failure | OBSERVED | counted across all committed specs: 69 multi-assertion tests, 5 with any message | high | New gate rule `unexplained-failure`; all 36 findings paid down |
| The analyzer treated fixture strings as real code | OBSERVED | flagged `test('proves nothing')` and a `waitForTimeout` that existed only inside quoted fixtures | high | Masks strings and comments before locating tests; false positives train people to ignore a gate |
| Masking then blinded the selector rule | OBSERVED | `unmarked-fragile-selector` stopped firing on a real `.locator('.a > div:nth-child(3)')` | high | Rules judge the masked copy; the selector rule reads raw at the same offset |
| The triage agent works against the live API | OBSERVED | `npm run triage -- examples/failure-403.json` returned classification=infrastructure, high confidence, citing the real 403 from the input. $0.0998 | high | First real agent execution in this project |
| Role `testability-reviewer` runs end to end and its claims are true | OBSERVED | 10 turns, $0.2388, 95s. Reported 11 interactive / 0 testId / 5 stable / 6 text-dependent / 1 form / 0 tables and named all 5 ids and 6 link labels — every value matches scans/timer.json exactly | high | It ran the scan; it did not confabulate |
| The role found two defects the scanner cannot see | OBSERVED | no aria-live/role on #javascript_countdown_time; no aria-pressed anywhere; verified by fetching the live page independently | high | Step 3 of the testability-audit skill, applied unprompted |
| Nothing loaded .env | OBSERVED | no dotenv dependency, no --env-file, no loadEnvFile anywhere; documented vars were silently ignored | high | Fixed with src/env.ts using process.loadEnvFile; no dependency added |
| The SDK throws on maxTurns instead of yielding a result | OBSERVED | 'Reached maximum number of turns (12)' escaped as an unhandled crash | high | The budget's stop path had never been exercised. Now returns a partial result with stoppedBy set |
| An agent wrapped its report in a code fence with preamble | OBSERVED | check-report: 'No YAML frontmatter block found' on the first attempt | medium | The checker caught it. Role prompt now states the file must begin with --- and carry no fence |
| The countdown suite gated on a decoy value | OBSERVED | `beforeEach` asserted only `toBeVisible()`, but `#javascript_countdown_time` ships in the static HTML already reading `01:01:12` — the exact value the inline init script sets, so the gate could not tell a live app from an unparsed one | high | One external run failed on `reset mid-countdown`; the click hit a button whose listener had not been attached yet |
| The readiness fix was verified by reproduction, not by absence of failure | OBSERVED | aborting `**/countdown.js` makes the new gate fail with its own diagnostic and `Received: "01:01:12"`; the old `toBeVisible()` gate passed that same page | high | Gate now waits for the app global, then advances one virtual second to prove the interval ticks |
| The suite header contradicted its own tests | OBSERVED | header claimed reset "leaves the timer paused"; app source shows `resetTimer()` never touches the interval, and two tests assert run state is preserved | medium | Corrected; a wrong comment outlives the person who wrote it |
| The scanner collected network bodies and threw them away | OBSERVED | `src/cli/scan.ts` reduced every call to {method, path, status}; NetworkRecorder had captured full request/response bodies | high | `schema.ts` now infers a data dictionary — types, nullable, optional, real samples — from the same traffic |
| A jQuery page was classified as an SPA | OBSERVED | first stack run on testpages.eviltester.com reported rendering=spa on evidence of jQuery 3.7.1 alone | high | Library presence says nothing about rendering. Added an `enhanced` tier; correct on both apps after |
| Occlusion detection flagged a control our own suite clicks every run | OBSERVED | `Reset Timer` reported unreachable; countdown-timer specs click it successfully in every green run | high | My clamping sampled a point off the element for anything below the fold. Off-screen is not a blocker at all — Playwright auto-scrolls. Now judged only where observable |
| The testability audit raised a finding per element on any app without test ids | OBSERVED | old auditTestability returned a medium for every text-dependent element; countdown-timer has 11 interactive and 0 test ids | high | Re-centred on identify/interact/observe. Same page now yields 2 findings, both real |
| The scanner never crossed into iframes and said nothing about it | OBSERVED | querySelectorAll does not enter frames; a framed app would report as a near-empty clean page | high | Frames are now listed and raise an `unscanned-frame` finding. Silence read as coverage |
| The `unlabelled-input` finding was unreachable | OBSERVED | unit test failed: an unlabelled input is also positionally addressed, so the generic branch caught it first and continued | medium | Found by writing the test, not by reading the code. Specific findings now precede generic ones |
| The network log writes real payload bodies to disk | OBSERVED | `todo.network.ndjson` contained the literal todo text; grep confirmed | high | Gated by environment: on test/prod bodies are recorded as withheld. Verified by grepping a prod-policy log for payload text and finding none |
| No code reads a scan back | OBSERVED | grep for `scans/` across src/ returns only two prompt strings in roles.ts | high | Scans are write-only artefacts. No baseline for healing, no drift detection, no accumulation across sessions |
| Self-healing does not exist in any form | OBSERVED | no implementation matches heal/fuzzy/similarity/fingerprint in src/; README says "no self-healing", CLAUDE.md says "planned" | high | Honestly reported, entirely absent. Blocked on element identity (E3) |
| `testability-audit` contradicted the tool it documents | OBSERVED | skill still instructed agents to raise "add data-testid" after the scanner stopped raising it | medium | Two sources of truth means one is lying. Skill rewritten in the same change |
